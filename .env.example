# Overnight Newsroom — environment variables (no secrets here)

# Local Convex deployment URL (set in .env.local)
VITE_CONVEX_URL=

# Ingestion API secret (set in .env.local, no default)
INGESTION_API_SECRET=
